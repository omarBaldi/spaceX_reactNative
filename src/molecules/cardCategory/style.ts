import { StyleSheet } from 'react-native';

export const cardCategoryStyle = StyleSheet.create({
  mainContainer: {
    width: '100%',
    height: 200,
    overflow: 'hidden',
    borderRadius: 10,
  },
  cardImage: {
    flex: 1,
  },
});
